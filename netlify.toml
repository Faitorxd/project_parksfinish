[build]
  command = "CI=false DISABLE_ESLINT_PLUGIN=true npm install && chmod +x node_modules/.bin/react-scripts && npm run build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200